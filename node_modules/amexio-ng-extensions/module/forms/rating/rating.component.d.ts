import { EventEmitter, OnInit } from '@angular/core';
import { AbstractControl, ControlValueAccessor, Validator } from '@angular/forms';
export declare class AmexioRatingComponent implements OnInit, ControlValueAccessor, Validator {
    iconclass: string;
    fullicon: string;
    emptyicon: string;
    readonly: boolean;
    disabled: boolean;
    required: boolean;
    float: boolean;
    fieldlabel: string;
    titles: string[];
    max: number;
    onHover: EventEmitter<{}>;
    onLeave: EventEmitter<{}>;
    model: number;
    ratingRange: number[];
    hovered: number;
    hoveredPercent: number;
    private _max;
    private onChange;
    private onTouched;
    writeValue(value: number): void;
    registerOnChange(fn: any): void;
    registerOnTouched(fn: any): void;
    validate(c: AbstractControl): {
        required: boolean;
    };
    ngOnInit(): void;
    calculateWidth(item: number): number;
    setHovered(hovered: number): void;
    changeHovered(event: MouseEvent): void;
    resetHovered(): void;
    rate(value: number): void;
    private buildRanges();
    private range(start, end);
}
